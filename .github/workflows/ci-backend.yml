# ============================================================================
# Tendex AI - Backend CI Pipeline
# ============================================================================
# Triggers on every Pull Request that modifies backend code.
# Steps: restore dependencies, build, run unit tests, run integration tests.
# NOTE: This pipeline is CI only. CD (deployment) is deferred to Sprint 7.
# ============================================================================

name: CI - Backend (.NET)

on:
  pull_request:
    branches:
      - main
      - develop
    paths:
      - "backend/**"
      - ".github/workflows/ci-backend.yml"
  push:
    branches:
      - main
      - develop
    paths:
      - "backend/**"
      - ".github/workflows/ci-backend.yml"

concurrency:
  group: ci-backend-${{ github.ref }}
  cancel-in-progress: true

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  SOLUTION_PATH: backend/TendexAI.slnx

jobs:
  # ====================================================================
  # Job 1: Build & Unit Tests
  # ====================================================================
  build-and-unit-test:
    name: Build & Unit Tests (.NET 10)
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      # ------------------------------------------------------------------
      # 1. Checkout repository
      # ------------------------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # ------------------------------------------------------------------
      # 2. Setup .NET SDK
      # ------------------------------------------------------------------
      - name: Setup .NET 10 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      # ------------------------------------------------------------------
      # 3. Cache NuGet packages
      # ------------------------------------------------------------------
      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('backend/**/*.csproj') }}
          restore-keys: |
            nuget-${{ runner.os }}-

      # ------------------------------------------------------------------
      # 4. Restore dependencies
      # ------------------------------------------------------------------
      - name: Restore dependencies
        run: dotnet restore ${{ env.SOLUTION_PATH }}

      # ------------------------------------------------------------------
      # 5. Build solution (Release mode, treat warnings as errors)
      # ------------------------------------------------------------------
      - name: Build solution
        run: dotnet build ${{ env.SOLUTION_PATH }} --configuration Release --no-restore --warnaserror

      # ------------------------------------------------------------------
      # 6. Run unit tests (exclude integration tests)
      # ------------------------------------------------------------------
      - name: Run unit tests
        run: |
          dotnet test ${{ env.SOLUTION_PATH }} \
            --configuration Release \
            --no-build \
            --verbosity normal \
            --filter "FullyQualifiedName!~IntegrationTests" \
            --logger "trx;LogFileName=unit-test-results.trx" \
            --collect:"XPlat Code Coverage"

      # ------------------------------------------------------------------
      # 7. Upload unit test results
      # ------------------------------------------------------------------
      - name: Upload unit test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-results
          path: |
            backend/**/TestResults/**/*.trx
            backend/**/TestResults/**/*.xml
          retention-days: 14

  # ====================================================================
  # NOTE: Integration Tests have been moved to a manual workflow.
  # They require Docker + Testcontainers (SQL Server, Redis, RabbitMQ)
  # which can be flaky in GitHub Actions runners.
  # Run integration tests manually via: gh workflow run ci-backend.yml
  # or through the GitHub Actions UI.
  # ====================================================================
