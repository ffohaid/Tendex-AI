# Initial analysis for 04052026 issues

## Scope summary

The attached document reports **12 issues** concentrated around the booklet creation flow from template, booklet editor state management, AI assistant field behavior, validation UX, and sidebar/header UI consistency.

## Issue clusters

| Cluster | Issues | Initial technical focus |
|---|---|---|
| Navigation and active state | 1, 3, 4, 12 | Router state, sidebar active matching, editor mode persistence across refresh, section change scroll reset, creation-flow edit-mode defaults |
| Header and action visibility | 2, 8 | Template creation header layout composition, conditional rendering of download action, spacing/alignment regression |
| AI assistant field fidelity | 5, 7 | Field-scoped prompts, HTML/table serialization, rich content rendering in current-content preview |
| Form and validation behavior | 6, 9, 10, 11 | Read-only system fields, validation state cleanup, first-error scroll/focus, date rule corrections around award/start dates |

## Business interpretation

The document indicates that the current problems are not isolated defects; several of them suggest duplicated state or inconsistent source-of-truth boundaries between creation flow, editor view mode, field-level AI tooling, and validation layers. The fixes must therefore prioritize centralization and deterministic derivation over local component patches.

## High-risk areas to inspect first

1. Sidebar menu item active-state computation and route watchers.
2. Create-from-template entry flow and booklet editor mode initialization/persistence.
3. Template editor header composition, including conditional action buttons.
4. AI assistant service/prompt builder and field content serialization for tables.
5. Central validation schema and shared scroll-to-error behavior.
6. Date rule implementation for release date and work start date.

## Expected affected frontend areas

Likely candidates include sidebar/menu components, booklet creation views, booklet editor view/components, AI assistant modal/composable/service, shared validation utilities, and RFP/template-related schemas.
